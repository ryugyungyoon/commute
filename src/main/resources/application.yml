spring:
  thymeleaf:
    prefix: classpath:templates/
    suffix: .html
  datasource:
    url: "jdbc:mysql://localhost/commute"
    username: "root"
    password: "0000"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
